git add . 

git commit -m "message" 

git status

git log --oneline 



 a new branch, create a new feature, when done merge to main branch 

#create a new branch 
git branch feature-branch

#check for branches 
git branch 

#switch to another branch 
git checkout feature-button  


#check main branch
git branch main


#Merge to a branch, first go to that branch where you want to merge 
git checkout main 
git merge feature-another-button
git push origin main 
#now delete the feature button 
git branch -d feature-another-button 
#then push
git push origin main

#delete feature branch on remote repo 
git push origin --delete feature-another-button 
git merge feature-search 

#delete the feature-search locally and remotely 
git branch -d feature-search 
git push origin --delete feature-search 